require 'rails_helper'

RSpec.describe "home/index.html.haml" do
  
  # it "renders the HTML template" do
  #   render
  #   expect(rendered).to match /HTML/
  # end

  it "displays a new poll form"
  #   assigns(:poll, Poll.create!(title: "What's your favourite color?"))
  #   render
  #   expect(rendered).to match /favourite color/
  # end
end
